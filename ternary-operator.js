// The ternary operator is a concise way to write conditional statements in JavaScript
// SYNTAX : condition ? expressionIfTrue : expressionIfFalse

const age = 18;

const canVote = age >= 18 ? "Yes, Person can Vote" : "No, Cannot Vote"

console.log(canVote);